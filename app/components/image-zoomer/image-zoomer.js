'use strict';

angular.module('furnitureWallApp.image-zoom-directive]', [])
    .directive('imageZoom', [function() {
        return {
            restrict: 'E',
            templateUrl: 'image-zoomer.html',
            link: function(scope,el,attr){
                
            }

        };
    }]);
